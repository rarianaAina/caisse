node_modules/
dist/
build/
coverage/
.turbo/
*.tsbuildinfo

# env — les fichiers d'exemple sont versionnés, les vrais jamais
.env
.env.local
.env.*.local
.env.production
!.env.production.example

# tauri / rust — le motif couvre les DEUX applications (caisse, outil
# d'émission) : les nommer une par une, c'est oublier la prochaine.
apps/*/src-tauri/target/
apps/*/src-tauri/gen/

# bases locales de dev
*.db
*.db-shm
*.db-wal

# éditeurs / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/
